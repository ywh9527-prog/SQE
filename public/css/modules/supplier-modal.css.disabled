/* 供应商资料管理模态框样式 */
.modal {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(0, 0, 0, 0.5);
  z-index: 1000;
  display: flex;
  align-items: center;
  justify-content: center;
}

/* 🎯 [CSS-CLEANUP] 移除BEM模态框选择器冲突 - 2025-12-20
   问题：.supplier-modal样式与supplier-modals-bem.css冲突
   解决：移除冲突样式，统一使用BEM CSS系统
   注意：.supplier-modal样式已迁移到supplier-modals-bem.css
*/

.modal-content {
  background: white;
  border-radius: 8px;
  width: 90%;
  max-width: 600px;
  max-height: 90vh;
  overflow: hidden;
  box-shadow: 0 4px 20px rgba(0, 0, 0, 0.15);
}

.modal-header {
  padding: 20px;
  border-bottom: 1px solid #e5e7eb;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.modal-header h3 {
  margin: 0;
  font-size: 18px;
  font-weight: 600;
  color: #1f2937;
}

/* 上传模态框头部操作区域 */
.supplier-modal__header-actions {
  display: flex;
  align-items: center;
  gap: 8px;
  margin-left: auto;
  margin-right: 12px;
}

.supplier-modal__header-actions .document-type-settings-btn {
  background: linear-gradient(135deg, #6b7280 0%, #4b5563 100%);
  color: white;
  border: none;
  padding: 6px 12px;
  border-radius: 6px;
  font-size: 12px;
  font-weight: 500;
  cursor: pointer;
  transition: all 0.2s ease;
  display: inline-flex;
  align-items: center;
  gap: 4px;
}

/* 资料类型设置按钮样式 - 参考构成选择按钮设计 */
.add-document-type-btn {
  background: linear-gradient(135deg, #9d7a54 0%, #8b6942 100%);
  color: white;
  border: none;
  padding: 8px 16px;
  border-radius: 6px;
  font-size: 13px;
  font-weight: 500;
  cursor: pointer;
  transition: all 0.2s ease;
  display: inline-flex;
  align-items: center;
  gap: 6px;
  margin-top: 8px;
  width: 100%;
  justify-content: center;
}

.add-document-type-btn:hover {
  background: linear-gradient(135deg, #8b6942 0%, #7a5c3a 100%);
  transform: translateY(-1px);
  box-shadow: 0 2px 4px rgba(157, 122, 84, 0.2);
}

.add-document-type-btn:active {
  transform: translateY(0);
  box-shadow: 0 1px 2px rgba(157, 122, 84, 0.1);
}

.modal-close {
  background: none;
  border: none;
  font-size: 24px;
  cursor: pointer;
  color: #6b7280;
  padding: 0;
  width: 32px;
  height: 32px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 4px;
  transition: all 0.2s;
}

.modal-close:hover {
  background-color: #f3f4f6;
  color: #374151;
}

.modal-body {
  padding: 20px;
  max-height: calc(90vh - 140px);
  overflow-y: auto;
}

.modal-footer {
  padding: 20px;
  border-top: 1px solid #e5e7eb;
  display: flex;
  justify-content: flex-end;
  gap: 12px;
}

/* 表单样式 */
.form-group {
  margin-bottom: 16px;
}

.form-group label {
  display: block;
  margin-bottom: 6px;
  font-weight: 500;
  color: #374151;
}

.form-group input[type="text"],
.form-group input[type="date"],
.form-group select,
.form-group textarea {
  width: 100%;
  padding: 10px 12px;
  border: 1px solid #d1d5db;
  border-radius: 6px;
  font-size: 14px;
  transition: border-color 0.2s;
}

.form-group input:focus,
.form-group select:focus,
.form-group textarea:focus {
  outline: none;
  border-color: #3b82f6;
  box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.1);
}

.form-group input[readonly] {
  background-color: #f9fafb;
  color: #6b7280;
}

.form-group input[type="checkbox"] {
  margin-right: 8px;
}

.expiry-date {
  margin-top: 8px;
}

/* 上传区域样式 */
.upload-drop-zone {
  border: 2px dashed #d1d5db;
  border-radius: 8px;
  padding: 32px;
  text-align: center;
  cursor: pointer;
  transition: all 0.2s;
  background-color: #f9fafb;
}

.upload-drop-zone:hover {
  border-color: #3b82f6;
  background-color: #eff6ff;
}

.upload-drop-zone.dragover {
  border-color: #3b82f6;
  background-color: #dbeafe;
}

.upload-icon {
  font-size: 48px;
  margin-bottom: 12px;
}

.upload-drop-zone p {
  margin: 0;
  color: #6b7280;
  font-size: 14px;
}

.file-preview {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 12px;
  background-color: #f0f9ff;
  border: 1px solid #bfdbfe;
  border-radius: 6px;
  margin-top: 8px;
}

.file-name {
  font-size: 14px;
  color: #1e40af;
}

.remove-file {
  background: none;
  border: none;
  color: #ef4444;
  cursor: pointer;
  font-size: 18px;
  padding: 4px;
  border-radius: 4px;
  transition: background-color 0.2s;
}

.remove-file:hover {
  background-color: #fee2e2;
}

/* 按钮样式 */
.btn {
  padding: 10px 20px;
  border: none;
  border-radius: 6px;
  font-size: 14px;
  font-weight: 500;
  cursor: pointer;
  transition: all 0.2s;
}

.btn-primary {
  background-color: #3b82f6;
  color: white;
}

.btn-primary:hover {
  background-color: #2563eb;
}

.btn-secondary {
  background-color: #6b7280;
  color: white;
}

.btn-secondary:hover {
  background-color: #4b5563;
}

/* 详情区域头部样式 */
.section-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 12px;
}

.section-header h4 {
  margin: 0;
  color: #1f2937;
}

.section-actions {
  display: flex;
  gap: 8px;
}

.upload-btn {
  background-color: #10b981;
  color: white;
  border: none;
  padding: 6px 12px;
  border-radius: 4px;
  font-size: 12px;
  cursor: pointer;
  transition: background-color 0.2s;
}

.upload-btn:hover {
  background-color: #059669;
}

.delete-material-btn {
  background-color: #9d7a54;
  color: white;
  border: none;
  padding: 6px 12px;
  border-radius: 4px;
  font-size: 12px;
  cursor: pointer;
  transition: background-color 0.2s;
}

.delete-material-btn:hover {
  background-color: #8b6939;
}

/* 邮件按钮样式 */
.email-btn {
  background-color: #3b82f6;
  color: white;
  border: none;
  padding: 6px 12px;
  border-radius: 4px;
  font-size: 12px;
  cursor: pointer;
  transition: background-color 0.2s;
}

.email-btn:hover {
  background-color: #2563eb;
}

.batch-email-btn {
  background-color: #8b5cf6;
  color: white;
  border: none;
  padding: 6px 12px;
  border-radius: 4px;
  font-size: 12px;
  cursor: pointer;
  transition: background-color 0.2s;
  margin-right: 8px;
}

.batch-email-btn:hover {
  background-color: #7c3aed;
}

/* 邮件预览模态框样式 */
.email-modal-content {
  max-width: 800px;
  width: 90%;
}

.email-subject {
  margin-bottom: 20px;
}

.email-subject label {
  display: block;
  margin-bottom: 6px;
  font-weight: 500;
  color: #374151;
}

.email-subject input {
  width: 100%;
  padding: 10px 12px;
  border: 1px solid #d1d5db;
  border-radius: 6px;
  font-size: 14px;
  font-weight: 600;
  background-color: #f9fafb;
}

.email-content {
  margin-bottom: 20px;
}

.email-content label {
  display: block;
  margin-bottom: 6px;
  font-weight: 500;
  color: #374151;
}

.email-content textarea {
  width: 100%;
  padding: 12px;
  border: 1px solid #d1d5db;
  border-radius: 6px;
  font-size: 14px;
  line-height: 1.6;
  font-family: 'Microsoft YaHei', Arial, sans-serif;
  resize: vertical;
  background-color: #f9fafb;
  white-space: pre-wrap;
}

/* 新增物料按钮样式 */
.add-material-btn {
  width: 100%;
  background-color: #f3f4f6;
  color: #374151;
  border: 2px dashed #d1d5db;
  padding: 16px;
  border-radius: 6px;
  font-size: 14px;
  cursor: pointer;
  transition: all 0.2s;
  margin-top: 8px;
}

.add-material-btn:hover {
  background-color: #e5e7eb;
  border-color: #9ca3af;
}

/* 文档操作按钮样式 */
.doc-actions {
  display: flex;
  gap: 4px;
  margin-left: auto;
}

.action-btn {
  background: none;
  border: none;
  cursor: pointer;
  padding: 4px;
  border-radius: 4px;
  font-size: 14px;
  transition: background-color 0.2s;
}

.edit-btn:hover {
  background-color: #fef3c7;
}

.delete-btn:hover {
  background-color: #f3f4f6;
}

/* 搜索和筛选控件样式 - 玻璃态现代设计 */
.supplier-controls {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 20px;
  padding: 20px;
  background: rgba(255, 255, 255, 0.15);
  backdrop-filter: blur(10px);
  border-radius: 16px;
  gap: 20px;
  border: 1px solid rgba(255, 255, 255, 0.2);
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
}

.search-section {
  position: relative;
  flex: 1;
  max-width: 400px;
}

.search-input {
  width: 100%;
  padding: 12px 40px 12px 16px;
  border: 1px solid rgba(255, 255, 255, 0.3);
  border-radius: 12px;
  font-size: 14px;
  background: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(5px);
  color: #374151;
  transition: all 0.3s cubic-bezier(0.23, 1, 0.32, 1);
  box-sizing: border-box;
}

.search-input::placeholder {
  color: rgba(55, 65, 81, 0.6);
}

.search-input:focus {
  outline: none;
  border-color: rgba(59, 130, 246, 0.5);
  background: rgba(255, 255, 255, 0.2);
  box-shadow:
    0 0 0 4px rgba(59, 130, 246, 0.1),
    0 4px 16px rgba(0, 0, 0, 0.1);
  transform: translateY(-1px);
}

.clear-search-btn {
  position: absolute;
  right: 12px;
  top: 50%;
  transform: translateY(-50%);
  background: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(3px);
  border: 1px solid rgba(255, 255, 255, 0.2);
  color: rgba(55, 65, 81, 0.7);
  cursor: pointer;
  font-size: 16px;
  width: 28px;
  height: 28px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 8px;
  transition: all 0.3s cubic-bezier(0.23, 1, 0.32, 1);
}

.clear-search-btn:hover {
  background: rgba(239, 68, 68, 0.1);
  border-color: rgba(239, 68, 68, 0.3);
  color: #ef4444;
  transform: translateY(-50%) scale(1.1);
}

.filter-section {
  display: flex;
  gap: 16px;
  align-items: center;
}

/* 🎯 [CSS-REFACTOR] 移除重复的.filter-select定义 - 2025-12-20
   原定义已迁移到 supplier-search-professional.css 作为权威来源
   遵循 claude.md 模块化隔离原则，避免样式冲突
*/

/* 无数据状态 */
.no-data {
  text-align: center;
  padding: 60px 20px;
}

.no-data-content {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 16px;
}

.no-data-icon {
  font-size: 48px;
  color: #9ca3af;
}

.no-data p {
  margin: 0;
  color: #6b7280;
  font-size: 16px;
}

/* 汇总信息 */
.supplier-summary {
  padding: 16px;
  background-color: #f0f9ff;
  border: 1px solid #bfdbfe;
  border-radius: 6px;
  margin-top: 16px;
  font-size: 14px;
  color: #1e40af;
  text-align: center;
}

.highlight {
  font-weight: 600;
  color: #1e40af;
}

/* 无资料提示样式 */
.no-documents-hint {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 12px;
  padding: 20px;
  background-color: #f8fafc;
  border: 1px dashed #d1d5db;
  border-radius: 6px;
  color: #6b7280;
  font-size: 14px;
  margin-top: 12px;
}

.hint-icon {
  font-size: 24px;
  opacity: 0.7;
}

.hint-text {
  font-style: italic;
}

/* 响应式设计 */
@media (max-width: 768px) {
  .modal-content {
    width: 95%;
    margin: 10px;
  }

  .modal-header,
  .modal-body,
  .modal-footer {
    padding: 16px;
  }

  .upload-drop-zone {
    padding: 24px;
  }

  .upload-icon {
    font-size: 36px;
  }

  .section-header {
    flex-direction: column;
    align-items: flex-start;
    gap: 8px;
  }

  .section-actions {
    width: 100%;
  }

  .upload-btn {
    flex: 1;
  }

  .no-documents-hint {
    flex-direction: column;
    text-align: center;
    gap: 8px;
  }
}

  .supplier-controls {
    flex-direction: column;
    gap: 12px;
  }

  .search-section {
    max-width: none;
  }

  .filter-section {
    width: 100%;
    justify-content: space-between;
  }

  /* 🎯 [CSS-REFACTOR] 移除重复的.filter-select定义 - 2025-12-20 */
}